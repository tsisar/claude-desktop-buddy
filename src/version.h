#pragma once

// Firmware version, reported in the cmd:status ack ("fw") so the desktop
// bridge can tell which protocol quirks to expect. Bump on releases and
// record the changes in CHANGELOG.md.
#define BUDDY_FW_VERSION "0.9.0"
